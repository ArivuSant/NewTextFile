#include <string>
#include <fstream>
using namespace std;

class textFile {
	
	// Member variables
	string sName; // File name
	string sLocation; // file location
	int iSize; // file size
	fstream fsFilePointer;

	textFile(string filePath) {
		
	}
};